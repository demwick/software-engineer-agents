<!--
  software-engineer-agents
  Copyright (C) 2026 demwick
  Licensed under the GNU Affero General Public License v3.0 or later.
  See LICENSE in the repository root for the full license text.
-->

# State file layout — v2.0.0 audit

**Last updated:** 2026-04-15
**Companion to:** `docs/specs/2026-04-15-scope-and-state-refactor.md`
**Status:** Phase 6 executed. Opportunities 1 and 2 from the "Known simplification opportunities" section have been applied:
  - `.needs-verify` is now existence-only; retry counter lives in a new `.verify-attempts` file (Opportunity 1).
  - `state.json.schema_version` bumped from 1 to 2 with auto-migration in `scripts/state-update.sh` (Opportunity 1 companion).
  - State paths owned by the deleted v1 commands (`phases/phase-N/review.md`, `reviews/ad-hoc-<timestamp>.md`, `ship-report.json`, `ship/<category>.log`, `debug/session-<N>/*`) are marked **v1-only, deprecated in v2.0.0** in the inventory table below (Opportunity 2). Opportunities 3–5 remain deferred.

This document is the canonical reference for every file the plugin writes inside a project's `.sea/` directory (plus the per-agent `MEMORY.md` files the platform manages outside `.sea/`). For each file it records the writer(s), reader(s), format, required fields, behavior on missing or corrupted state, and how it can be recreated.

Phase 2 of the scope-cut refactor produced this audit. The v1.0.0 reference document is preserved verbatim at the bottom of this file under "v1.0.0 reference (historical)".

---

## Scope

Two storage layers are in play:

1. **Project runtime state** — under `<project-root>/.sea/`. Gitignored by default. Regenerated by `/sea-init` if lost. Contains the roadmap, per-phase plans and progress, transient Stop-hook markers, and (for `/sea-diagnose`) health-audit output. State paths owned by v1 commands that v2.0.0 deleted (`/sea-review`, `/sea-debug`, `/sea-ship`) still exist on disk in migrated projects but have no writer or reader in v2.0.0 — they are inert artifacts that `/sea-init --fresh` or manual cleanup can remove whenever the user chooses.
2. **Per-agent cross-session memory** — under `.claude/agent-memory/<agent>/MEMORY.md`. Platform-managed via the subagent `memory: project` frontmatter field. Not gitignored by default; commit or ignore per project preference.

The per-file detail section below covers both layers. The cross-file invariants apply to layer 1 only — the platform owns layer 1 invariants for `MEMORY.md`.

## Convention

Paths below use `<project>` for the project root, `N` for a phase number, `<N>` for a debug session index (also an integer), and `<timestamp>` / `<YYYYMMDD>` for ISO-like timestamp suffixes the writing code generates at runtime.

---

## File inventory

One row per state artifact. "Optional?" answers *"can this file legitimately be absent in a valid project state?"* "Recreated by" answers *"if I delete it, what regenerates it — if anything?"*

| File | Path | Written by | Read by | Format | Required fields | Optional? | Recreated by |
|---|---|---|---|---|---|---|---|
| `state.json` | `<project>/.sea/state.json` | `/sea-init` (create); `scripts/state-update.sh` (update, called by `sea-go`, `sea-quick`, `sea-roadmap`); `hooks/state-tracker` (last_edit) | `sea-status`, `sea-go`, `sea-quick`, `sea-diagnose`, `hooks/session-start`, `hooks/state-tracker` | JSON | `schema_version` (**= 2** in v2.0.0), `mode`, `created`, `current_phase`, `total_phases` | **No** — its absence means "uninitialized project" | Re-run `/sea-init` |
| `roadmap.md` | `<project>/.sea/roadmap.md` | `/sea-init` (create, via `planner`); `/sea-roadmap` (edit, including milestone flow); `/sea-go` (status flip on phase complete) | `sea-status`, `sea-go`, `sea-quick`, `hooks/session-start` | Markdown (structured H2/H3 headings) | Project header + at least one `### Phase N:` section with a `**Status:**` line | **No** — every initialized project has one | Re-run `/sea-init` |
| `diagnose.json` | `<project>/.sea/diagnose.json` | `/sea-diagnose` | `sea-status`, `sea-quick` (to route diagnose-sourced tasks) | JSON | `generated`, `focus`, `priority_actions` | **Yes** — absent when `/sea-diagnose` has never run | Re-run `/sea-diagnose` |
| `.needs-verify` | `<project>/.sea/.needs-verify` | `/sea-go` and `/sea-quick` via `: > .sea/.needs-verify` (touch); `hooks/auto-qa` deletes on every terminal state | `hooks/auto-qa` (existence-only; content ignored in v2) | Empty file (v2). Legacy v1 files may still contain a single integer — the hook tolerates this via its v1 fallback branch. | — | **Yes** — only present between executor finish and the next `auto-qa` terminal state | Any subsequent `/sea-go` or `/sea-quick` recreates it |
| `.verify-attempts` | `<project>/.sea/.verify-attempts` | `hooks/auto-qa` only, via atomic `jq` + `mv` from a temp file | `hooks/auto-qa` (reads on next hook invocation) | JSON: `{"attempts": N}` where N is 0-2. | `attempts` | **Yes** — created by the hook on first failure, deleted on every terminal state. Never created by any skill. | Next failed retry in the current auto-QA cycle |
| `.last-verify.log` | `<project>/.sea/.last-verify.log` | `hooks/auto-qa` | `sea-status`, humans reading stderr | Plain text (stdout + stderr of the last test run, optionally followed by a `=== host-compat warning ===` trailer) | — | **Yes** — absent until `auto-qa` runs at least once | Any `auto-qa` run |
| `phases/phase-N/plan.md` | `<project>/.sea/phases/phase-N/plan.md` | `planner` agent (Mode B) via `/sea-go` | `executor` agent (via `/sea-go`, `/sea-quick`); `sea-status`; `sea-go` (re-read on resume) | Markdown (structured `## Tasks` with `### Task K` subsections) | `## Tasks` section with at least one `### Task` | **Yes** — only present for phases that have been planned; `/sea-go` creates on demand | `/sea-go` on the same phase |
| `phases/phase-N/progress.json` | `<project>/.sea/phases/phase-N/progress.json` | `executor` agent (one write per successful task commit) | `/sea-go` (resume check); `executor` (next-turn resume) | JSON | `phase`, `current_task`, `completed_tasks[]`, `last_commit`, `updated` | **Yes** — absent for a fresh phase (task 1 not yet done), or after phase completes (deleted) | `/sea-go` on a partially-completed phase writes a new one as work proceeds |
| `phases/phase-N/summary.md` | `<project>/.sea/phases/phase-N/summary.md` | `/sea-go` at phase completion | `sea-status` | Markdown (free-form with `Completed:`, `Commits:`, `Files touched:`, `Notes:` lines) | `Completed:` timestamp | **Yes** — absent until the phase is `done` | Not auto-recreated |
| `phases/phase-N/summary.md.reverted-<timestamp>` | same | **v1-only, deprecated in v2.0.0** — was written by the deleted `/sea-undo` skill | Humans only (audit trail) | Markdown (same shape as `summary.md`) | — | **Yes** — no writer in v2; inert artifact in migrated projects | Not regenerated in v2 |
| `phases/phase-N/review.md` | `<project>/.sea/phases/phase-N/review.md` | **v1-only, deprecated in v2.0.0** — was written by the deleted `reviewer` agent via `/sea-review` or `/sea-go` Step 6.5 | Humans only | Markdown (5-axis structured report) | — | **Yes** — no writer in v2; inert artifact in migrated projects. v2 delegates code review to `addyosmani/agent-skills:code-review`. | Not regenerated in v2 |
| `reviews/ad-hoc-<timestamp>.md` | `<project>/.sea/reviews/ad-hoc-<timestamp>.md` | **v1-only, deprecated in v2.0.0** — was written by the deleted `reviewer` agent via `/sea-review <range>` | Humans only | Markdown | — | **Yes** — no writer in v2; inert artifact | Not regenerated in v2 |
| `phases/archived-<YYYYMMDD>-phase-N/` | `<project>/.sea/phases/archived-<YYYYMMDD>-phase-N/` | `/sea-roadmap` (rename on phase delete/reorder); `/sea-init` (when re-scaffolding an existing project) | Humans only (audit) | Directory mirroring `phases/phase-N/` at the moment of archival | — | **Yes** — only present after archival | Not regenerated; preserved indefinitely |
| `debug/session-<N>/reproduction.md` | `<project>/.sea/debug/session-<N>/reproduction.md` | **v1-only, deprecated in v2.0.0** — was written by the deleted `debugger` agent via `/sea-debug` | Humans only | Markdown | — | **Yes** — no writer in v2; inert artifact. v2 delegates debugging to `obra/superpowers:debugging` / `addyosmani/agent-skills:debugging`. | Not regenerated in v2 |
| `debug/session-<N>/root-cause.md` | same directory | **v1-only, deprecated in v2.0.0** | Humans only | Markdown | — | **Yes** — inert | Not regenerated in v2 |
| `debug/session-<N>/experiments.md` | same directory | **v1-only, deprecated in v2.0.0** | Humans only | Markdown | — | **Yes** — inert | Not regenerated in v2 |
| `ship-report.json` | `<project>/.sea/ship-report.json` | **v1-only, deprecated in v2.0.0** — was written by the deleted `/sea-ship` skill | Humans, CI | JSON | — | **Yes** — inert artifact. v2 delegates pre-merge gate to `addyosmani/agent-skills:shipping`. | Not regenerated in v2 |
| `ship/<category>.log` | `<project>/.sea/ship/<category>.log` | **v1-only, deprecated in v2.0.0** | Humans | Plain text | — | **Yes** — inert | Not regenerated in v2 |
| `.claude/agent-memory/<agent>/MEMORY.md` | `<project-or-global>/.claude/agent-memory/<agent>/MEMORY.md` | The named subagent, managed by Claude Code's `memory: project` frontmatter field | The same subagent on its next invocation (platform-loaded, first 25KB) | Markdown (platform-owned shape) | Platform-defined | **Yes** — platform auto-creates on first use | Platform recreates on next invocation; losing it loses accumulated learnings |

**Nineteen entries total** — eighteen files/directories under `.sea/` (one new: `.verify-attempts`, added in v2.0.0) plus one platform-managed location under `.claude/agent-memory/`. Eight of the `.sea/` entries are **v1-only** (deprecated in v2.0.0): the six `review.md` / `reviews/` / `debug/session-<N>/*` / `ship-report.json` / `ship/<category>.log` / `summary.md.reverted-<timestamp>` paths previously owned by deleted commands. v2 readers ignore them; v2 writers never produce them.

---

## Per-file detail

The inventory table above is the index. The per-file sections below answer four questions for each entry:

- **Writer(s)** — exact skill, agent, hook, or script that writes it, with file paths.
- **Reader(s)** — every consumer, with file paths.
- **What happens if it is missing?** — does the plugin degrade, recover, or crash?
- **What happens if it is corrupted?** — who detects, how does recovery work?

### `state.json`

- **Writer(s):**
  - `skills/sea-init/SKILL.md` (initial `Write`, the only place raw `Write` is permitted on this file).
  - `scripts/state-update.sh` (every subsequent update — atomic `jq`-based merge via a temp file, `mv`-into-place, preserves required fields, validates schema after merge). Called from `skills/sea-go/SKILL.md:102`, `skills/sea-quick/SKILL.md`, `skills/sea-milestone/SKILL.md`, `skills/sea-roadmap/SKILL.md`.
  - `hooks/state-tracker` (PostToolUse) refreshes `last_edit` via the same helper.
- **Reader(s):**
  - `skills/sea-status/SKILL.md:25-37`, `skills/sea-go/SKILL.md:24`, `skills/sea-quick/SKILL.md`, `skills/sea-milestone/SKILL.md:24`, `skills/sea-diagnose/SKILL.md:92`, `skills/sea-undo/SKILL.md`.
  - `hooks/session-start` (SessionStart) reads on every session launch for the `additionalContext` payload.
- **Missing:** the plugin treats this as "uninitialized project" — `/sea-go`, `/sea-quick`, `/sea-status`, `/sea-milestone` all halt and route the user to `/sea-init`. `SessionStart` hook injects an empty `additionalContext`.
- **Corrupted:** `scripts/state-update.sh` fails closed. `jq` parse error → exit 4, merge aborted, the existing file is left untouched. If the *on-disk* file is corrupted (hand-edited into invalid JSON), the next `state-update` call refuses to merge and the user must restore from git or re-run `/sea-init` with `--fresh`.

### `roadmap.md`

- **Writer(s):** `/sea-init` (initial, via `planner` Mode A); `/sea-roadmap` (edits); `/sea-go` (flips Phase N status to `done` at phase completion, see `skills/sea-go/SKILL.md:109`); `/sea-milestone` (appends new phases below a milestone boundary marker).
- **Reader(s):** `/sea-status` (phase count), `/sea-go` (next-pending-phase pick), `/sea-quick` (consistency check), `/sea-milestone`, `/sea-review` (phase-range lookup), `hooks/session-start` (short state summary in context).
- **Missing:** same as `state.json` — project is "uninitialized". `/sea-roadmap` specifically refuses to create it; only `/sea-init` does.
- **Corrupted:** no automated detection. Readers parse what they can; `/sea-status` will show a degraded count. Hand-editing is *expected*, so schema drift is mostly tolerated — only `### Phase N:` headings and `**Status:**` lines are load-bearing.

### `diagnose.json`

- **Writer(s):** `skills/sea-diagnose/SKILL.md:79` writes the full report after the researcher agent completes its audit.
- **Reader(s):** `skills/sea-status/SKILL.md:41` (header), `skills/sea-quick/SKILL.md:28-93` (suggests re-running `/sea-diagnose` after a fix if the task came from `priority_actions`).
- **Missing:** expected — `/sea-status` simply omits the "last audit" line; `/sea-quick` skips the post-fix reminder.
- **Corrupted:** no automated detection. `/sea-status` falls back to "last audit: unparseable" and moves on. No other caller hard-depends on structure.

### `.needs-verify` (v2.0.0 existence-only)

- **Writer(s):**
  - `skills/sea-go/SKILL.md` and `skills/sea-quick/SKILL.md` touch the marker with `: > .sea/.needs-verify` after the executor finishes. The file is empty by design — v2.0.0 no longer writes any content into it.
  - `hooks/auto-qa` deletes it on every terminal state: pass, loop-protection give-up, hard give-up after 2 retries, host-compat failure, and when no test runner is detected.
- **Reader(s):** `hooks/auto-qa` (existence check only — content ignored in v2). Plus the companion reference doc `skills/sea-go/references/auto-qa-protocol.md`.
- **Missing:** this is the *normal* state. Its presence means "the executor just finished writing code; the Stop hook should run tests".
- **Corrupted:** not possible — content is not read in v2. The hook retains a v1 fallback that reads an integer from the marker if `.verify-attempts` is absent, so legacy fixtures and in-flight migrated projects keep working across the schema v1 → v2 rollover.

### `.verify-attempts` (new in v2.0.0)

- **Writer(s):** `hooks/auto-qa` only. Writes `{"attempts": N}` via `jq` to a `mktemp` file, then `mv`s it into place on every failed retry. Skills must not touch this file.
- **Reader(s):** `hooks/auto-qa` on the next invocation — reads `.attempts` with a `// 0` default so a missing file is treated as zero retries so far.
- **Missing:** expected until the first failed retry inside an auto-QA cycle, and again after the cycle completes (hook deletes the file on every terminal state).
- **Corrupted:** `hooks/auto-qa` parses with `jq -r '.attempts // 0'` which falls back to `0` on non-JSON or missing-key content. Defensive — a corrupted counter cannot lock the user out; the worst case is one extra retry before the give-up branch kicks in.

### `.last-verify.log`

- **Writer(s):** `hooks/auto-qa:57,60,69` (redirects test command stdout/stderr, appends host-compat trailer).
- **Reader(s):** `skills/sea-status/SKILL.md:43` (surfaces mtime + tail), `skills/sea-debug/SKILL.md:28,47` (error bait for triage), `tests/run-tests.sh:191` (regression assertion on host-compat warning content).
- **Missing:** expected before the first Stop-hook run. `/sea-status` omits the last-run line; `/sea-debug` skips the "last auto-QA run failed X minutes ago" nudge.
- **Corrupted:** not possible in the usual sense — the file is a raw dump of another process's output. Consumers only read it as opaque text (mtime, last ~30 lines, substring match).

### `phases/phase-N/plan.md`

- **Writer(s):** `planner` agent in Mode B (Phase Planning), called by `skills/sea-go/SKILL.md:41`. The planner is the only writer.
- **Reader(s):** `executor` agent (`agents/executor.md:28`) for task-by-task execution; `skills/sea-go/SKILL.md:38` (plan-presence check, re-reads on resume); `skills/sea-status/SKILL.md:28` (optional active-phase display).
- **Missing:** `skills/sea-go/SKILL.md:41` treats "no plan" as a signal to launch the planner, so missing-by-design. Missing on a phase the planner previously wrote is an anomaly — `/sea-go` will regenerate it, losing any `[[ ASK: ... ]]` decisions the user already answered.
- **Corrupted:** no automated validation. The executor is tolerant of minor structure drift but will surface `_common.md:2` "Manage Confusion Actively" if the file is so broken it cannot identify tasks, and stop the line.

### `phases/phase-N/progress.json`

- **Writer(s):** `agents/executor.md:34,39,54-58` — one atomic write after each successful task commit inside a phase.
- **Reader(s):** `skills/sea-go/SKILL.md:59` (resume decision); `agents/executor.md:29` (skip completed tasks); `skills/sea-debug/SKILL.md:29` (detect blocked phase); `skills/sea-undo/SKILL.md:31,89` (fallback when summary is missing, plus deletion on revert).
- **Missing:** normal for a fresh phase (nothing started yet) or a completed phase (deleted at phase end). `/sea-go` interprets "missing" as "start at task 1".
- **Corrupted:** `/sea-go` and the executor refuse to parse non-JSON and fall back to "start at task 1". Silent data loss risk: if `completed_tasks[]` is lost, the executor re-runs tasks — benign because each task is an atomic, idempotent commit-or-skip, but worth flagging.

### `phases/phase-N/gate-pending.json` (new in v2.1.0)

- **Writer(s):** `agents/executor.md` when a task whose id appears in the plan's `risk_gates` section is reached. Executor writes this file, marks the task `gated` in `progress.json`, and exits with `STATUS: gate`.
- **Reader(s):** `skills/sea-go/SKILL.md` (Step 5 "Resume after gate" branch) reads the marker to surface the confirmation prompt to the user. Deleted by the executor on the next invocation once "gate resumed" context is passed in.
- **Format:**
  ```json
  {
    "phase": <N>,
    "task": <task id>,
    "kind": "<gate kind>",
    "confirmation_prompt": "<text from plan>",
    "created": "<ISO UTC>"
  }
  ```
- **Required fields:** all of the above. Missing `kind` or `confirmation_prompt` is treated as a corrupt marker by `/sea-go`, which falls back to re-reading the plan's `risk_gates` section.
- **Invariants:** exists **iff** the executor exited with `STATUS: gate` and has not yet been re-launched with a resume context. Clearing on resume is the executor's responsibility; manual deletion unblocks the phase at the user's risk.
- **Missing:** normal in every phase where no gate has been hit. A missing marker after a `STATUS: gate` exit is an anomaly — `/sea-go` re-reads the plan and re-surfaces the gate from `risk_gates` directly.
- **Corrupted:** `/sea-go` refuses to auto-confirm; surfaces the plan's `risk_gates` entry and asks the user to confirm from the plan text instead of the marker.

### `phases/phase-N/summary.md`

- **Writer(s):** `skills/sea-go/SKILL.md:110` writes the summary when the phase completes successfully.
- **Reader(s):** `skills/sea-status/SKILL.md` (historical display), `skills/sea-review/SKILL.md:33` (phase-range pickup), `skills/sea-undo/SKILL.md:30` (commit range extraction).
- **Missing:** the phase is treated as "not yet completed" by every reader. `/sea-undo` falls back to `progress.json` + `git log`. `/sea-review` cannot target the phase.
- **Corrupted:** `/sea-undo`'s commit-range regex fails closed and surfaces an error rather than attempting a wrong revert.

### `phases/phase-N/summary.md.reverted-<timestamp>`

- **Writer(s):** `skills/sea-undo/SKILL.md:88` renames the original `summary.md` to this path when the phase is reverted — never deletes history.
- **Reader(s):** humans only. No code reads this file.
- **Missing:** expected in every unreverted phase.
- **Corrupted:** not possible — it is a rename, so the file is whatever `summary.md` was at the moment of the revert.

### `phases/phase-N/review.md`

- **Writer(s):** `agents/reviewer.md:86,94` writes the full markdown report from `/sea-review` or `/sea-go` Step 6.5 (on medium/complex phases after auto-QA passes).
- **Reader(s):** humans; `/sea-review` re-reads for repeated runs.
- **Missing:** expected for phases that have not been reviewed. No caller crashes on absence.
- **Corrupted:** no automated detection. Humans re-run `/sea-review` to overwrite.

### `reviews/ad-hoc-<timestamp>.md`

- **Writer(s):** `agents/reviewer.md` (via `/sea-review <range>` for range-based, non-phase reviews).
- **Reader(s):** humans only.
- **Missing:** expected by default — ad-hoc reviews are opt-in.
- **Corrupted:** humans re-run.

### `phases/archived-<YYYYMMDD>-phase-N/`

- **Writer(s):** `skills/sea-roadmap/SKILL.md` and `skills/sea-init/SKILL.md` rename an old `phases/phase-N/` directory to this form when the phase is removed or the project is re-scaffolded. Spec design: never delete, always archive.
- **Reader(s):** humans only.
- **Missing:** expected in projects that have never had a phase removed.
- **Corrupted:** not possible — archival is a directory rename.

### `debug/session-<N>/*.md`

- **Writer(s):** `agents/debugger.md:31,154` writes `reproduction.md`, `root-cause.md`, or `experiments.md` depending on triage outcome.
- **Reader(s):** humans; `skills/sea-debug/SKILL.md:36` increments `<N>` by counting existing session directories.
- **Missing:** expected — debug sessions are opt-in.
- **Corrupted:** session directories are append-only; humans re-run `/sea-debug` to produce a new session.

### `ship-report.json` and `ship/<category>.log`

- **Writer(s):** `skills/sea-ship/SKILL.md:59,101` writes the overall report and per-category logs after running the pre-merge quality gate.
- **Reader(s):** humans, optionally CI.
- **Missing:** expected until the first `/sea-ship` run.
- **Corrupted:** humans re-run `/sea-ship`.

### `.claude/agent-memory/<agent>/MEMORY.md`

- **Writer(s):** the named subagent, via the platform's `memory: project` mechanism. The plugin itself never touches this path.
- **Reader(s):** the same subagent on its next invocation. The platform auto-loads up to 25 KB.
- **Missing:** expected on first invocation; platform creates on demand.
- **Corrupted:** the platform handles recovery. SEA does not own this invariant.

---

## Cross-file invariants

These rules must hold across multiple state files. They are not enforced by a single validator — each reader relies on its own narrow check — but they are the assumptions the plugin's design rests on. A violation is a bug.

1. **`state.json.current_phase` is in-range.** `0 ≤ current_phase ≤ total_phases`. `total_phases` equals the count of `### Phase N:` sections in `roadmap.md`. Violation → `/sea-go` either picks a non-existent phase or refuses to advance.

2. **Active progress implies the active phase.** If `.sea/phases/phase-N/progress.json` exists, then `state.json.current_phase == N`. Put differently: there is at most one phase with an in-flight `progress.json`. Violation → executor and `/sea-go` disagree about which phase to resume.

3. **Summary exists only for completed phases.** `.sea/phases/phase-N/summary.md` exists if and only if `roadmap.md` marks Phase N as `done` (or `/sea-undo` has reverted the phase, in which case a `.reverted-<timestamp>` sibling replaces it). Violation → `/sea-status`, `/sea-review`, `/sea-undo` produce inconsistent views.

4. **`schema_version` is monotonic.** `state.json.schema_version` never decreases. All migrations are one-way. `scripts/state-update.sh` does not support downgrades; the `pre-scope-cut` git tag is the only rollback floor.

5. **Required `state.json` fields survive every update.** `schema_version`, `mode`, `created`, `current_phase`, `total_phases` are all always present after any `scripts/state-update.sh` call (exit 4 if the merge drops one). Violation can only happen via a raw `Write`/`Edit` on an existing `state.json`, which is explicitly forbidden in `CLAUDE.md:104`.

6. **`.needs-verify` existence is session-bounded.** If `.sea/.needs-verify` is present at the start of a new Claude Code session, either the previous session crashed before `hooks/auto-qa` could clear it, or the last `auto-qa` run hit loop-protection and gave up. In either case the presence represents "unfinished verification work" and `/sea-status` should surface it. (In practice no current reader does — this is a known follow-up.)

7. **`.verify-attempts` implies `.needs-verify` (v2.0.0).** If `.sea/.verify-attempts` exists, `.sea/.needs-verify` must also exist. The counter file is meaningless without the marker; `hooks/auto-qa` clears both on every terminal state and only creates `.verify-attempts` while the marker is present. A `.verify-attempts` file without its marker sibling is a corruption and should be treated as stale.

8. **`progress.json` self-consistency.** `progress.json.phase == N` for the containing `phases/phase-N/` directory, and `progress.json.current_task` > every element of `progress.json.completed_tasks[]`. Violation → executor re-runs tasks unnecessarily (benign) or skips unfinished tasks (not benign).

9. **Phase archival is append-only.** A directory renamed to `phases/archived-<YYYYMMDD>-phase-N/` is never reopened. `/sea-roadmap` refuses to resurrect archived phases; the user must re-add as a new phase number.

### What happens if `state.json` and `roadmap.md` disagree?

The most common real-world case this spec must answer.

| Disagreement | Detected by | Effect | Recovery |
|---|---|---|---|
| `state.json.current_phase > total_phases_in_roadmap` | `/sea-go` (picks a phase that does not exist in `roadmap.md`) | `/sea-go` halts with "phase N not found in roadmap" | User hand-edits `roadmap.md` to add the missing phase, or runs `/sea-roadmap` |
| `state.json.current_phase` points to a phase whose `roadmap.md` status is `done` | `/sea-go` (refuses to rerun a completed phase) | `/sea-go` halts with "Phase N is already done" | User bumps `current_phase` via `/sea-roadmap` or deletes the stale entry |
| `state.json.current_phase` points to a phase whose `roadmap.md` status is `pending`, but `phases/phase-N/summary.md` exists | `/sea-go` (inconsistency between summary and roadmap status) | Invariant 3 violated — `/sea-go` halts and surfaces `_common.md:2` "Manage Confusion Actively" | User either flips roadmap status to `done` or archives the stray summary |
| `total_phases` in `state.json` ≠ count of `### Phase N:` in `roadmap.md` | `/sea-status` (phase count mismatch) | `/sea-status` shows the roadmap count as authoritative and warns the user | User runs `/sea-roadmap` to re-sync, or edits `state.json` via `scripts/state-update.sh total_phases=K` |

In every case the plugin **stops the line rather than guessing**, per `_common.md:5`. There is no auto-reconciliation. This is deliberate: silently trusting one file over the other is the path to data corruption. Phase 6 may add a single-purpose validator skill to detect these drifts proactively, but it is not in scope for the current refactor.

---

## Known simplification opportunities for v2.0.0

> **Sign-off gate.** Phase 6 of the refactor (state consolidation) does not start until the user has re-read this section and explicitly confirmed the scope. See `docs/specs/2026-04-15-scope-and-state-refactor.md:267` and `docs/specs/2026-04-15-scope-and-state-refactor.md:456-461`.

Five candidates are documented below. Each has a **Recommendation** line that is either *Do in Phase 6*, *Defer* (track but do not execute in v2.0.0), or *Reject* (do not do). Two are recommended for Phase 6; three are deferred to a follow-up state-consolidation pass after v2.0.0 has been lived with.

### Opportunity 1 — Split `.needs-verify` content-as-retry-counter into two files

- **Files affected:** `<project>/.sea/.needs-verify`, new `<project>/.sea/.verify-attempts`. Code: `hooks/auto-qa`, `skills/sea-go/SKILL.md:80`, `skills/sea-quick/SKILL.md:78`, `skills/sea-go/references/auto-qa-protocol.md`, eval fixtures under `evals/fixtures/` and `evals/suites/hooks/auto-qa-*.sh` that `echo` into `.needs-verify` today.
- **Simplification:** `.needs-verify` becomes an existence-only flag (zero-byte or any content, only its presence is checked). Retry counting moves to a dedicated `.verify-attempts` file written atomically via `jq` to a temp-then-mv. Clears both on success, loop-protection, or host-compat give-up. Removes the `cat $MARKER → integer` overload flagged in `CLAUDE.md:103-104`.
- **What breaks:**
  - `hooks/auto-qa:30` `ATTEMPTS=$(cat "$MARKER")` must become a read of `.verify-attempts` with a missing-file default of `0`.
  - `hooks/auto-qa:94` `echo "$NEXT" > "$MARKER"` must become an atomic `jq`-and-mv into `.verify-attempts`.
  - The loop-protection branch at `hooks/auto-qa:35` needs to read `.verify-attempts` alongside `stop_hook_active`.
  - `skills/sea-go/SKILL.md:80` and `skills/sea-quick/SKILL.md:78` must stop writing `0` into `.needs-verify`; they `touch` it instead.
  - Three eval suites under `evals/suites/hooks/` that inject retry counts into `.needs-verify` via `echo` must be updated to inject into `.verify-attempts`.
  - The companion doc `skills/sea-go/references/auto-qa-protocol.md` must be updated to describe the two-file scheme.
- **User-visible behavior change:** none. The Stop-hook retry loop, block reasons, and give-up messages remain bit-identical. The only change is *where* the integer lives.
- **Migration cost:** ~50 lines of bash across `hooks/auto-qa`, two 1-line edits in the two skills, 3 eval-suite updates, a new migration-regression eval under `evals/suites/state/` or `evals/suites/hooks/`. Total: ~1 working session, mostly eval writing.
- **Recommendation:** **Do in Phase 6.** This is the refactor review's lead finding and the Phase 6 spec's lead bullet (`docs/specs/2026-04-15-scope-and-state-refactor.md:463-471`). It pairs naturally with the `state.json schema_version` bump to 2 — ship as one BREAKING state-schema change with a single migration path.

### Opportunity 2 — Mark state paths for deleted commands as v1-only in docs

- **Files affected:** `.sea/phases/phase-N/review.md`, `.sea/reviews/ad-hoc-<timestamp>.md`, `.sea/ship-report.json`, `.sea/ship/<category>.log`, `.sea/debug/session-<N>/*.md`. Code: `docs/STATE.md` (this file), `docs/migration/v1-to-v2.md` (created in Phase 8).
- **Simplification:** These paths have no writer in v2 (their writing skills — `/sea-review`, `/sea-ship`, `/sea-debug` — are deleted in Phase 3). Every new v2 project will never have them. Migrated v1 projects may still contain them and that is fine: v2 readers do not consume them, so they are effectively inert artifacts that `/sea-init --fresh` or manual `rm -rf` can clean up whenever the user chooses. Nothing to migrate — just mark them clearly as "v1-only, deprecated in v2.0.0" in `docs/STATE.md`'s inventory table and the migration guide.
- **What breaks:** nothing. This opportunity is documentation-only.
- **Migration cost:** ~10 lines of doc edits. Zero code.
- **Recommendation:** **Do in Phase 6** (bundle with the schema_version bump). Keep it in the same PR so the migration guide describes the whole state-model delta in one place. Zero code risk, high clarity value.

### Opportunity 3 — Collapse `state.json.total_phases` duplication

- **Files affected:** `<project>/.sea/state.json` (drop `total_phases`), `<project>/.sea/roadmap.md` (sole source of truth). Code: `scripts/state-update.sh` (remove `total_phases` from `REQUIRED`), `skills/sea-init/SKILL.md:77-96`, every reader that currently reads `state.json.total_phases` (cross-ref via `grep -rn 'total_phases'`), plus the cross-file invariant #1 above.
- **Simplification:** `total_phases` is read from both `state.json` and `roadmap.md` today, and the two can drift. Making `roadmap.md` the sole source — i.e., `total_phases := count of "### Phase N:" headings in roadmap.md` — removes invariant #1 entirely (no more "are these two files in sync?" question) and shrinks `state.json`'s required-field list from 5 to 4.
- **What breaks:**
  - `scripts/state-update.sh:91` `REQUIRED` list, its validation exit 4, and all eval suites that assert `state.json.total_phases` is present (at least `evals/suites/state/update-rejects-missing-schema-version.sh` and probably a neighbor).
  - `skills/sea-init/SKILL.md` must stop writing `total_phases` on create.
  - Every reader that cited `state.json.total_phases` must switch to parsing `roadmap.md` (adds a ~10-line bash helper or re-uses an existing one).
  - `docs/STATE.md` invariants section must drop #1 and cite `roadmap.md` as authoritative.
- **User-visible behavior change:** very small — `/sea-status` reads the same number from a different file. Stale `state.json` files from v1 would need the field stripped on migration (trivial `jq del`).
- **Migration cost:** ~2 working sessions. Mostly eval rewriting and reader-audit sweeps. Low code risk but high surface area.
- **Recommendation:** **Defer.** Worth doing but not worth bundling into v2.0.0 alongside the `.needs-verify` split and the command-surface cut. Track as a v2.1 candidate. Revisit after v2.0.0 has been lived in for 2–4 weeks (same cadence as the deferred 6 → 3 command cut).

### Opportunity 4 — Merge `phases/phase-N/progress.json` into `state.json.active_phase`

- **Files affected:** `<project>/.sea/phases/phase-N/progress.json` (delete file), `<project>/.sea/state.json` (add `active_phase: { phase, current_task, completed_tasks, last_commit, updated }` sub-object). Code: `agents/executor.md:29,34,39,54-58`, `skills/sea-go/SKILL.md:59`, `skills/sea-debug/SKILL.md:29`, `skills/sea-undo/SKILL.md:31,89`, the `scripts/state-update.sh` required-field set, and every eval referring to `progress.json`.
- **Simplification:** Merges one more state file into the canonical `state.json`. Removes invariant #2 ("active progress implies the active phase") because the progress object is nested under the phase that owns it. Simplifies resume logic from "check two files" to "check one field".
- **What breaks:**
  - Executor's atomic-write sequence must funnel through `scripts/state-update.sh` (or a new sibling helper) rather than a direct `jq > progress.json`. This is a nontrivial correctness change — the progress file is written on a hot path after every task commit, and the state-update helper currently does schema validation that may be overkill for an inner-loop write.
  - `/sea-undo` fallback path loses a cheap file-existence test and must parse the JSON structure instead.
  - `/sea-debug` heuristic ("phase has `progress.json` still present" → "executor was mid-phase and didn't finish cleanly") must be rewritten against `state.json.active_phase.current_task > 0 && active_phase.updated < now - threshold`.
- **User-visible behavior change:** resume-after-crash still works, but the debug detection heuristic changes shape.
- **Migration cost:** ~3 working sessions. This is the most invasive opportunity — touches the executor's write path, and the executor is load-bearing. Not a Phase 6 candidate unless the user explicitly wants it.
- **Recommendation:** **Defer.** Correct direction, wrong time. Track as a v2.2 candidate. Revisit only after the post-v2.0.0 review confirms the `.needs-verify` split landed cleanly and the state-update helper is up to the extra traffic.

### Opportunity 5 — Segregate transient artifacts under `.sea/logs/`

- **Files affected:** `<project>/.sea/.last-verify.log` and `<project>/.sea/phases/phase-N/summary.md.reverted-<timestamp>`. Code: `hooks/auto-qa:37,57,69`, `skills/sea-undo/SKILL.md:88`, plus any reader that cites the old paths (`skills/sea-status/SKILL.md:43`, `skills/sea-debug/SKILL.md:28,47`, `tests/run-tests.sh:191`).
- **Simplification:** Today, truly ephemeral artifacts (a test-run log, a reverted-summary sibling) sit alongside canonical state inside `.sea/` and `.sea/phases/phase-N/`. Moving them under a dedicated `.sea/logs/` subdirectory makes the distinction between "state" and "logs" visible to a human walking the tree with `ls`. Small comprehension win, zero behavioral change.
- **What breaks:** about eight path references. One test assertion in `tests/run-tests.sh:191`. No evals unless we add a new one asserting the `.sea/logs/` directory is created.
- **Migration cost:** ~1 working session, mostly sed-and-review. Trivial migration — on upgrade, the logs are regenerated on the next run so there is no data to move.
- **Recommendation:** **Defer.** Real improvement but cosmetic. Bundle with whichever future refactor touches `hooks/auto-qa` next. Not worth a dedicated PR.

### Summary

| # | Opportunity | Recommendation | Where |
|---|---|---|---|
| 1 | Split `.needs-verify` into flag + counter | **Do** | Phase 6 |
| 2 | Mark dead-command paths as v1-only in docs | **Do** | Phase 6 |
| 3 | Drop `state.json.total_phases` duplication | Defer | v2.1 |
| 4 | Merge `progress.json` into `state.json.active_phase` | Defer | v2.2+ |
| 5 | Segregate transient artifacts under `.sea/logs/` | Defer | future |

**Phase 6 in-scope:** Opportunities 1 and 2. Everything else is deferred. The user signs off on this list before Phase 6 opens its branch.

---

## v1.0.0 reference (historical)

The text below is the original `docs/STATE.md` as shipped in v1.0.0 on 2026-04-14. It is preserved verbatim as a historical record. Where it disagrees with the audit above, the audit wins.

---

# State File Layout

This document describes what software-engineer-agents writes inside a project's `.sea/` directory and how each file is used. You don't need to create any of these by hand — `/sea-init` generates them. This doc is the reference when you want to inspect, tweak, or debug state.

## Directory Layout

```
<project-root>/
└── .sea/                # added to .gitignore automatically by /sea-init
    ├── state.json               # canonical runtime state
    ├── roadmap.md               # human-readable phase list
    ├── diagnose.json            # latest health report (if /sea-diagnose has run)
    ├── .needs-verify            # transient marker — triggers the auto-QA hook
    ├── .last-verify.log         # last test-runner output (written by auto-qa)
    └── phases/
        ├── phase-1/
        │   ├── plan.md          # planner's output for this phase
        │   └── summary.md       # executor's completion note
        ├── phase-2/
        │   ├── plan.md
        │   └── progress.json    # in-flight task progress (deleted on phase complete)
        └── archived-YYYYMMDD-phase-N/   # old plans, never deleted
```

## `state.json`

Canonical runtime state. Every command that mutates state updates this file. Schema:

```json
{
  "schema_version": 1,
  "mode": "from-scratch",
  "created": "2026-04-14T00:12:33Z",
  "current_phase": 2,
  "total_phases": 5,
  "last_session": "2026-04-14T03:47:01Z",
  "last_edit":    "2026-04-14T03:47:01Z",
  "last_commit":  "a1b2c3d"
}
```

| Field | Written by | Purpose |
|-------|-----------|---------|
| `schema_version` | `/sea-init` | Integer. Bump when state-shape changes; readers run migrations if older. |
| `mode` | `/sea-init` | `from-scratch` or `finish-existing` — affects /sea-go pipeline defaults |
| `created` | `/sea-init` | ISO-8601 UTC timestamp of initialization |
| `current_phase` | `/sea-go`, `/sea-roadmap` | Number of the active phase |
| `total_phases` | `/sea-init`, `/sea-roadmap` | Total phases in the roadmap |
| `last_session` | `/sea-go` end, `SessionStart` | Last time a command ran |
| `last_edit` | `PostToolUse` hook (`state-tracker`) | Last time any file was edited in this project by Claude |
| `last_commit` | `/sea-go` end, `/sea-quick` end | Short SHA of the most recent software-engineer-agents commit |

## `roadmap.md`

Human-readable phase list. The planner writes it, the user can hand-edit it, and `/sea-roadmap` edits it programmatically. Format:

```markdown
# Project Roadmap

## Project: <name>
## Created: <ISO date>
## Status: in-progress

## Phases

### Phase 1: <short name>
**Goal:** <one sentence>
**Scope:** <bullet list>
**Deliverable:** <what you end with>
**Depends on:** none | Phase X
**Status:** done | in-progress | pending

### Phase 2: ...
```

`Status` transitions: `pending` → `in-progress` → `done`. Once a phase is `done`, the `/sea-roadmap` skill refuses to remove or reorder it.

## `phases/phase-N/plan.md`

One plan file per phase, written by the `planner` agent in Mode B (Phase Planning) and consumed by the `executor`. Format:

```markdown
# Phase N Plan: <name>

## Context
<2-3 sentences>

## Complexity
trivial | medium | complex

## Pipeline
<describes which agents will be used>

## Tasks

### Task 1: <short name>
- **What:** <one sentence>
- **Files:** path1, path2 (new | modified)
- **Steps:**
  1. ...
  2. ...
- **Verification:** <exact command, expected output>
- **Commit:** `type(scope): message`

### Task 2: ...
```

If the planner cannot decide something, it inserts `[[ ASK: question ]]` markers. The `/sea-go` skill stops and surfaces these to the user before calling the executor.

## `phases/phase-N/progress.json` (transient)

Written by the executor after each successful task commit so the phase can resume after a crash, context reset, or `STATUS: blocked` exit.

```json
{
  "phase": 2,
  "current_task": 3,
  "completed_tasks": [1, 2],
  "last_commit": "a1b2c3d",
  "updated": "2026-04-14T05:21:00Z"
}
```

`/sea-go` checks this file before launching the executor:
- **Present** → resume context: skip `completed_tasks`, restart at `current_task`.
- **Absent** → fresh start at task 1.

Deleted automatically when the phase completes — `summary.md` becomes the historical record.

## `phases/phase-N/summary.md`

Written after the phase completes successfully. Short, appendable, human-readable:

```markdown
# Phase N Summary

Completed: <ISO now>
Commits: <count>  (<first-sha>..<last-sha>)
Files touched: <list>

Notes:
<2-3 sentences on what shipped, any deviations from the plan, anything the user should know>
```

## `diagnose.json`

Latest output from `/sea-diagnose`. Schema:

```json
{
  "generated": "2026-04-14T05:10:00Z",
  "focus": "all",
  "tests":    { "status": "warn", "findings": ["..."] },
  "errors":   { "status": "pass", "findings": [] },
  "security": { "status": "fail", "findings": ["..."] },
  "priority_actions": [
    "Add rate limiting to POST /api/login (src/routes/auth.ts:42)",
    "..."
  ]
}
```

Each section's `status` is one of `pass`, `warn`, `fail`. `/sea-status` reads this file to show the last audit result in its header.

## `.needs-verify` (transient)

A marker file created by `/sea-go` and `/sea-quick` after the executor finishes. Contains a single integer: the retry count (starts at `0`).

The `Stop` hook (`hooks/auto-qa`) checks for this file when Claude finishes responding:

1. No marker → hook exits 0, Claude stops normally
2. Marker exists → hook runs `scripts/detect-test.sh`, then runs the detected test command
3. Pass → marker is deleted, Claude stops
4. Fail → marker's counter is incremented, hook returns `{"decision": "block", "reason": "..."}`, Claude auto-retries the fix
5. After 2 failed retries → marker is deleted, hook returns a block with a "give up, report to user" reason

This file is transient: never commit it, never rely on its existence, never edit it by hand.

## `.last-verify.log` (transient)

Raw stdout + stderr of the last test run, written by `hooks/auto-qa`. Overwritten every run. Useful for debugging when auto-QA reports a failure — you can open it directly instead of re-running tests manually.

## Cross-Session Memory (NOT in `.sea/`)

Each subagent has its own `MEMORY.md` managed by the platform, stored under `.claude/agent-memory/<agent-name>/MEMORY.md`. This is distinct from `.sea/`:

| Location | Lifetime | Scope | Contents |
|----------|---------|-------|----------|
| `.sea/` | Project | Per-project runtime state | Roadmap, phase plans, current state |
| `.claude/agent-memory/<agent>/` | Agent, per project | Accumulated across sessions | Patterns, decisions, known pitfalls |

The platform loads the first 25KB of each `MEMORY.md` into the corresponding subagent at startup — we do not hand-manage this.

## Gitignore

`/sea-init` appends the following to `.gitignore` if it isn't already there:

```
# software-engineer-agents runtime state
.sea/
```

`.claude/agent-memory/` is NOT gitignored by default — agent learnings can legitimately be shared via git (they never contain secrets if the agent follows its rules). Decide per project whether to commit them.
